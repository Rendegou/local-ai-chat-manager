{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认权限：仅开放核心能力、目录选择与打开文件（最小权限原则）",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
